import React from "react";
import ReactDOM from "react-dom/client";
import ScreenStudio from "./components/screen-studio";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ScreenStudio />
  </React.StrictMode>
);
